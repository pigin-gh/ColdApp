package com.piginp.coldapp.domain.models

import java.util.Date

class DayEntry(val dayNumber: Int, val id: Int, val date: Date, val numberOfSymptoms: Int) // TODO: - Подумать над форматом даты (может быть баг, указал хз какой класс)